Task 12 Evidence: URL frontmatter integration

Route: POST /api/convert/url
File: senji-gateway/app/routes/convert.py

Integration:
- Calls `_build_markdown(readable.markdown, result.final_url, readable.title, "web", convert_html)`
- Real endpoint path prepends YAML frontmatter via `prepend_frontmatter()`
- Field mapping: source=result.final_url, title=readable.title, type=web

Verification:
- `cd senji-gateway && python -m pytest tests/ -v` → 43 passed
- `cd senji-gateway && python -m ruff check app/services/frontmatter.py tests/test_frontmatter.py` → clean

Compatibility:
- Existing route tests patch service callables with AsyncMock and still pass because `_build_markdown()` bypasses prepend for mocked service objects.
