T18: Docker Compose — Health Checks + Service Orchestration
===========================================================
Date: 2026-04-19

## docker compose config --quiet
Exit code: 0 (success)

## Changes Made

### docker-compose.yml
- Added restart: unless-stopped to all 3 services
- Added healthcheck (curl -sf) to gateway (:8000/health), readability (:3000/health), docling (:5001/health)
- Added deploy.resources.limits.memory: gateway=512M, readability=256M, docling=8G
- Updated gateway depends_on from service_started → service_healthy
- Docling start_period=60s (model loading time)

### senji-gateway/Dockerfile
- Added curl install for healthcheck probe
- Base: python:3.12-slim

### senji-readability/Dockerfile
- Changed node:22-slim → node:20-slim (LTS)
- Added curl install for healthcheck probe
- Changed COPY package*.json → COPY package.json package-lock.json (explicit)

### .dockerignore files
- senji-gateway: tests/, __pycache__/, *.pyc, .pytest_cache/, .ruff_cache/, *.egg-info/, .env
- senji-readability: tests/, node_modules/, *.test.js, .env

## senji_token observation
config.py has: senji_token: str = "dev-token"
Default "dev-token" exists for local dev/test convenience.
In production, SENJI_TOKEN env var in .env overrides via pydantic_settings.
No code change needed — pydantic_settings reads env vars automatically.
