services:
  gateway:
    build: ./senji-gateway
    ports:
      - "7878:8000"
    env_file: .env
    volumes:
      - vault:/opt/vault
    restart: unless-stopped
    depends_on:
      readability:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    mem_limit: 512m
    memswap_limit: 512m
    networks: [senji-net]

  readability:
    build: ./senji-readability
    expose: ["3000"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    mem_limit: 256m
    memswap_limit: 256m
    networks: [senji-net]

  obsidian-remote:
    image: ghcr.io/sytone/obsidian-remote:latest
    ports:
      - "7899:8080"
    volumes:
      - vault:/vaults
    environment:
      - PUID=1000
      - PGID=1000
    restart: unless-stopped
    mem_limit: 256m
    memswap_limit: 256m
    networks: [senji-net]

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "8082:8080"
    volumes:
      - open-webui-data:/app/backend/data
      - vault:/vault:ro
    environment:
      - OLLAMA_BASE_URL=http://10.1.1.222:11434
      - WEBUI_AUTH=true
    restart: unless-stopped
    mem_limit: 1g
    memswap_limit: 1g
    networks: [senji-net]

networks:
  senji-net:
    driver: bridge

volumes:
  open-webui-data:
  vault:
    driver: local
    driver_opts:
      type: nfs
      o: nfsvers=4,addr=10.1.1.250,hard,timeo=600,retrans=2,rw
      device: ":/local/dockerdata/vault"
